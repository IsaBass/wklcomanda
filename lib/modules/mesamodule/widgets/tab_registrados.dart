import 'package:flutter/material.dart';

class TabRegistrados extends StatelessWidget {
  const TabRegistrados({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Tab Registrados"),
      ),
    );
  }
}
